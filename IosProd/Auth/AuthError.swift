enum AuthError: String {
    case invalidEmail = "Invalid email format"
    case invalidPassword = "Password must be at least 6 characters"
    case invalidCredentials = "Invalid email or password"
}
