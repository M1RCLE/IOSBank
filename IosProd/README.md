# Экраны

## Экран авторизации

- Позволяет пользователю войти в систему

- Поля: Логин, Пароль

- Кнопка "Войти"

- Обработка ошибок

- Возможность восстановления пароля

## Экран списка услуг

- Показывает список доступных банковских услуг

- Отображение списка

- Возможность поиска услуг

- Фильтрация услуг по категориям

- Экран деталей услуги

- Показывает подробную информацию об услуге

- Детальное описание

- Возможность оформления

- Отзывы пользователей

- Поддержка чата с банком

## Экран пользователя

- Показывает информацию о пользователе и его счетах

- Отображение списка всех счетов (кредитных и дебетовых) с балансами

- Графическое представление баланса (графики, диаграммы)

- Кнопка "Перевести деньги"

- Форма для перевода средств между счетами

- История транзакций

## Экран истории транзакций

- Позволяет пользователю просматривать свои последние переводы и платежи

- Фильтрация по датам

- Поиск по сумме или получателю

- Детализация транзакции

## Экран настроек

- Позволяет пользователю управлять настройками приложения

- Изменение пароля

- Настройки безопасности (двухфакторная аутентификация)

- Настройка уведомлений



# Обработка жизненного цикла приложения

### Приложение уходит в фоновый режим

- Сохранение данных пользователя

- Закрытие активных соединений (например, с сервером)

- Вывод уведомления на экран "Сессия будет завершена через 5 минут бездействия" (не уверен)

### Приложение возвращается из фонового режима

- Проверка актуальности данных

- Обновление баланса на счетах

- Перенаправление на экран повторной авторизации, если сессия истекла

### Обработка разрыва интернет-соединения

- Вывод предупреждающего сообщения "Отсутствует интернет-соединение"

- Предложение перейти в офлайн-режим (если поддерживается)

### Приложение получает уведомление

- Показать баннер с уведомлением

- Если уведомление связано с транзакцией — предложить открыть соответствующую страницу истории транзакций

### Имеющиеся сущности

- Transaction

- BankAccount

- User

# Система Backend Driven UI (BDUI)

(не смог сам разобраться и попросил нейросеть сгенерировать объяснение в виде README 😁)

Гибкая система Backend Driven UI, которая позволяет рендерить UI-компоненты из JSON-данных. Система разработана для работы с существующими компонентами дизайн-системы и может быть легко расширена для поддержки новых компонентов.

## Обзор

Система BDUI состоит из следующих компонентов:

1. **BDUIElement** - Модель, представляющая UI-элемент с его типом, стилями, содержимым и дочерними элементами
2. **BDUIMapper** - Преобразует модели BDUIElement в экземпляры UIView на основе дизайн-системы
3. **BDUIActionHandler** - Обрабатывает действия, инициируемые UI-элементами (навигация, перезагрузка, закрытие, кастомные действия)
4. **BDUIViewController** - Контроллер представления, который рендерит UI из JSON-данных

## Структура JSON

JSON-структура для определения UI-элементов выглядит следующим образом:

```json
{
    "type": "contentView",
    "id": "uniqueId",
    "styles": {
        "backgroundColor": "#FFFFFF",
        "cornerRadius": "8",
        "padding": {
            "top": 16,
            "left": 16,
            "bottom": 16,
            "right": 16
        }
    },
    "content": {
        "key1": "value1",
        "key2": "value2"
    },
    "subviews": [
        // Дочерние элементы
    ],
    "actions": {
        "tap": {
            "type": "navigate",
            "payload": {
                "route": "details",
                "itemId": "12345"
            }
        }
    }
}
```

### Типы элементов

Система поддерживает следующие типы элементов:

- `button` - Кнопка с различными стилями
- `label` - Текстовая метка с различными стилями
- `textInput` - Поле ввода
- `card` - Контейнер в виде карточки
- `stackView` - Стек-вью для компоновки
- `contentView` - Универсальный контейнер

### Стили

Элементы могут иметь следующие стили:

- `backgroundColor` - Цвет фона в HEX-формате
- `cornerRadius` - Значение скругления углов
- `padding` - Значения отступов (top, left, bottom, right)

### Действия

Элементы могут выполнять следующие типы действий:

- `navigate` - Переход на другой экран
- `reload` - Перезагрузка текущего экрана или конкретного представления
- `dismiss` - Закрытие текущего экрана
- `custom` - Кастомное действие, обрабатываемое приложением

## Пример использования

```swift
// Создание BDUI-контроллера из JSON-строки
let jsonString = """
{
    "type": "contentView",
    "styles": {
        "backgroundColor": "#FFFFFF"
    },
    "subviews": [
        {
            "type": "label",
            "content": {
                "text": "Привет, мир!",
                "style": "title",
                "alignment": "center"
            }
        },
        {
            "type": "button",
            "content": {
                "title": "Нажми меня",
                "style": "primary"
            },
            "actions": {
                "tap": {
                    "type": "custom",
                    "payload": {
                        "name": "buttonTapped"
                    }
                }
            }
        }
    ]
}
"""

let bdViewController = BDUIViewController(jsonString: jsonString)
present(bdViewController, animated: true)
```

## Расширение системы

Для добавления поддержки нового UI-компонента:

1. Добавьте новый кейс в enum `ElementType`
2. Добавьте метод маппинга в класс `BDUIMapper`
3. Реализуйте компонент в дизайн-системе, если его нет

## Преимущества

- **Гибкость**: UI можно изменять без обновления приложения
- **Переиспользуемость**: Компоненты можно использовать повторно во всем приложении
- **Консистентность**: UI соответствует гайдлайнам дизайн-системы
- **Тестируемость**: UI можно тестировать с разными JSON-входами

